# https://github.com/google/yamlfmt/blob/main/docs/config-file.md
formatter:
  type: basic
  # yamlfmt の既定では空行が詰められてしまい、ステップ間を空行で区切っている
  # 既存ワークフローが全て lint エラーになるため、単一の空行を保持する
  retain_line_breaks_single: true
